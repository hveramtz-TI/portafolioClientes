# HU-023 — Eliminar o desactivar servicio (según historial de uso)

**Épica:** Rubros, categorías y servicios
**Prioridad:** Media
**Estado:** Aprobada
**Fecha:** 2026-08-19

## Descripción

Como usuario, quiero eliminar o desactivar un servicio de mi catálogo personal, para limpiarlo sin afectar órdenes históricas.

## Criterios de Aceptación

- [ ] Un servicio sin historial de uso en órdenes puede eliminarse definitivamente (`forceDelete()`).
- [ ] Un servicio con historial de uso en órdenes se desactiva (`status = 'desactivado'`); la orden conserva su copia histórica inmutable en `order_services`.
- [ ] El servicio desactivado no se muestra en perfil público ni se ofrece en nuevas solicitudes (scope `whereStatus('activo')`).
- [ ] La base se desactiva/reactiva solo mediante admin/seeder; el usuario propietario puede gestionar su fork mediante `/api/user-catalog/...`.
- [ ] Reactivar establece `status = 'activo'` solo en el servicio seleccionado; un padre desactivado mantiene la invisibilidad.

## Notas / Dudas

- `status` es una columna dedicada; `overrides` no contiene status. Para la base, al intentar eliminar se valida historial y forks; un fork se elimina por `/api/user-catalog/...`. La copia histórica en `order_services` siempre queda intacta.

## Flujos UX/UI relacionados

- `docs/flujos/servicio-crud.md`
- `docs/flujos/rubro-categoria-servicio-lifecycle.md`
