---
description: Run the frontend Jest test suite and verify it passes.
---

Run the frontend (Next.js) Jest tests and verify they pass.

1. Working directory: `frontend/`
2. Run `npm test` (append `$ARGUMENTS` when provided, e.g. a test path or `-t <name>`).
3. Report: if green, summary of suites/tests; if failures, the failing tests, assertion diff, root cause, and a minimal fix proposal.
4. Verification is read-only; do not modify code unless the user asks.
