---
description: Run ESLint on the frontend and verify zero errors.
---

Run ESLint and verify zero errors.

1. Working directory: `frontend/`
2. Run `npm run lint`.
3. Report error/warning counts; list `file:line` + rule for each error and propose a fix.
